---
phase: 16-tech-debt-closure
plan: 03
type: execute
wave: 2
depends_on: [16-01, 16-02]
files_modified:
  - platform-packages/darwin-arm64/better_sqlite3.node
autonomous: true
requirements: [DIST-04]

must_haves:
  truths:
    - "Platform package for darwin-arm64 contains the better_sqlite3.node binary"
    - "npm run build succeeds producing dist/server.mjs"
    - "node dist/server.mjs starts without import/module errors"
    - "tsc --noEmit passes with zero errors after Plans 01 and 02"
  artifacts:
    - path: "platform-packages/darwin-arm64/better_sqlite3.node"
      provides: "Native better-sqlite3 binary for macOS ARM"
    - path: "dist/server.mjs"
      provides: "Built MCP server entry point"
  key_links:
    - from: "platform-packages/darwin-arm64/package.json"
      to: "platform-packages/darwin-arm64/better_sqlite3.node"
      via: "files array includes better_sqlite3.node"
      pattern: "better_sqlite3\\.node"
---

<objective>
Build the darwin-arm64 platform package binary and run the final end-to-end smoke test: build the project, verify tsc --noEmit passes, and confirm the MCP server starts successfully.

Purpose: DIST-04 requires platform packages with extracted better-sqlite3 binaries. The smoke test proves all Phase 16 fixes work together.

Output: Platform binary for darwin-arm64, verified clean build, verified server startup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-tech-debt-closure/16-RESEARCH.md
@.planning/phases/16-tech-debt-closure/16-01-SUMMARY.md
@.planning/phases/16-tech-debt-closure/16-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build platform package and run full verification suite</name>
  <files>platform-packages/darwin-arm64/better_sqlite3.node</files>
  <read_first>
    - scripts/build-platform-packages.sh (understand the binary extraction process)
    - platform-packages/darwin-arm64/package.json (verify scaffolding exists)
    - package.json (verify build script and main field is dist/server.mjs after Plan 01)
    - tsconfig.json (verify compilation settings)
  </read_first>
  <action>
    **Step 1: Build the darwin-arm64 platform package**

    Run the existing build script to extract the better-sqlite3 native binary:
    ```bash
    chmod +x scripts/build-platform-packages.sh
    ./scripts/build-platform-packages.sh
    ```

    This copies `better_sqlite3.node` from `node_modules/better-sqlite3/build/Release/` into `platform-packages/darwin-arm64/`.

    **Step 2: Verify platform package**
    ```bash
    test -f platform-packages/darwin-arm64/better_sqlite3.node && echo "Binary exists" || echo "FAIL: binary missing"
    file platform-packages/darwin-arm64/better_sqlite3.node  # Should show Mach-O 64-bit
    ```

    **Step 3: Run full build**
    ```bash
    npm run build
    ```
    This runs tsdown and should produce `dist/server.mjs` among other outputs.

    **Step 4: Verify tsc --noEmit passes**
    ```bash
    npx tsc --noEmit
    ```
    Must exit 0 with no errors (all 24 errors fixed in Plan 02).

    **Step 5: Run full test suite**
    ```bash
    npx vitest run
    ```
    All tests must pass (including the new fork bomb test from Plan 01).

    **Step 6: MCP server startup smoke test**
    ```bash
    timeout 5 node dist/server.mjs 2>&1 || true
    ```
    The server will fail to bind to a transport (no stdin pipe in CLI), but it should NOT fail on import errors or missing modules. Check that the output does NOT contain "Cannot find module", "ERR_MODULE_NOT_FOUND", or "SyntaxError".

    **Note on other platforms:** The 3 remaining platform packages (darwin-x64, linux-x64, win32-x64) require their respective environments. Document this as a CI matrix build step. Only darwin-arm64 is built locally.
  </action>
  <verify>
    <automated>test -f platform-packages/darwin-arm64/better_sqlite3.node && npx tsc --noEmit 2>&1 | grep -c "error TS" | xargs -I{} sh -c 'if [ {} -ne 0 ]; then echo "FAIL: tsc errors remain"; exit 1; fi' && npm run build 2>&1 | tail -3 && test -f dist/server.mjs && echo "PASS: all checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - `platform-packages/darwin-arm64/better_sqlite3.node` exists and is a Mach-O binary
    - `npx tsc --noEmit` exits 0 with zero errors
    - `npm run build` succeeds without errors
    - `dist/server.mjs` exists after build
    - `npx vitest run` passes all tests
    - `node dist/server.mjs` does not emit "Cannot find module" or "ERR_MODULE_NOT_FOUND" in its output
  </acceptance_criteria>
  <done>darwin-arm64 platform package contains native binary. Full build succeeds. tsc clean. All tests pass. MCP server loads without import errors.</done>
</task>

</tasks>

<verification>
1. `test -f platform-packages/darwin-arm64/better_sqlite3.node` -- binary exists
2. `npx tsc --noEmit` -- zero errors
3. `npm run build` -- success
4. `npx vitest run` -- all tests pass
5. `node dist/server.mjs` -- no import errors in first 3 seconds
</verification>

<success_criteria>
- Platform binary built for darwin-arm64
- Zero TypeScript errors
- Clean build
- All tests pass
- Server starts without module resolution errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-tech-debt-closure/16-03-SUMMARY.md`
</output>
