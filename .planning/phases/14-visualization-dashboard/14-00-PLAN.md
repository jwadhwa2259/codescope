---
phase: 14-visualization-dashboard
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/dashboard/server.test.ts
  - tests/dashboard/api.test.ts
  - tests/dashboard/websocket.test.ts
autonomous: true
requirements:
  - VIZ-01
  - VIZ-06

must_haves:
  truths:
    - "Test stubs exist for server, API routes, and WebSocket before any implementation begins"
    - "Running vitest against tests/dashboard/ succeeds (stubs skip or pass trivially)"
  artifacts:
    - path: "tests/dashboard/server.test.ts"
      provides: "Stub test file for Hono server startup, static serving, route mounting"
      contains: "describe"
    - path: "tests/dashboard/api.test.ts"
      provides: "Stub test file for JSON API response shapes and error handling"
      contains: "describe"
    - path: "tests/dashboard/websocket.test.ts"
      provides: "Stub test file for WebSocket connection, event broadcasting, reconnection"
      contains: "describe"
  key_links:
    - from: "tests/dashboard/server.test.ts"
      to: "src/dashboard/server.ts"
      via: "imports startDashboard (will exist after Plan 01)"
      pattern: "server"
    - from: "tests/dashboard/api.test.ts"
      to: "src/dashboard/api/*.ts"
      via: "imports route handlers (will exist after Plan 01)"
      pattern: "api"
---

<objective>
Create test stub files for server-side dashboard concerns so that Nyquist sampling is satisfied from Wave 1 onward. These stubs define the test structure and expectations; Plan 01 tasks will populate the implementation that makes them pass.

Purpose: The VALIDATION.md contract requires test files to exist before implementation. Server-side concerns (Hono server, API routes, WebSocket) are testable with vitest. Client-side panels (sigma.js WebGL, html2canvas) are NOT vitest-testable per RESEARCH.md and use grep-based verification instead.

Output: 3 test stub files in tests/dashboard/ that run green (skipped or trivially passing) via vitest.
</objective>

<execution_context>
@/Users/jaywadhwa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaywadhwa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-visualization-dashboard/14-CONTEXT.md
@.planning/phases/14-visualization-dashboard/14-VALIDATION.md

<interfaces>
<!-- These interfaces define what the stubs will test once Plan 01 implements them -->

From src/dashboard/server.ts (to be created in Plan 01):
```typescript
export function startDashboard(port?: number): Server;
// Hono app with routes at /api/graph, /api/conventions, /api/readiness,
// /api/blast-radius/:file, /api/status, /api/review, /api/impact
// WebSocket at /ws
// Static file serving at /dashboard.js
```

API response envelope:
```typescript
interface ApiResponse<T> {
  status: 'ok' | 'error';
  data?: T;
  code?: string;
  message?: string;
}
```

WebSocket events:
```typescript
type WSEvent =
  | { type: 'bootstrap:progress'; stage: string; percentage: number }
  | { type: 'orient:phase'; phase: string }
  | { type: 'agent:spawn'; agent: string; wave: number }
  | { type: 'agent:complete'; agent: string; status: string }
  | { type: 'graph:updated' }
  | { type: 'readiness:snapshot' };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stub files for server, API, and WebSocket</name>
  <files>
    tests/dashboard/server.test.ts,
    tests/dashboard/api.test.ts,
    tests/dashboard/websocket.test.ts
  </files>
  <action>
Create the `tests/dashboard/` directory and three test stub files. Each file uses vitest `describe`/`it`/`it.skip` blocks that define the expected test structure. Tests that depend on Plan 01 implementation use `it.skip` with a clear comment: `// Enable after Plan 01 creates src/dashboard/server.ts`. Tests for pure logic that can be trivially asserted use `it` with a placeholder assertion.

**Create `tests/dashboard/server.test.ts`:**

```typescript
import { describe, it, expect } from 'vitest';

describe('Dashboard Server', () => {
  describe('startDashboard', () => {
    it.skip('starts Hono server on default port 7463', () => {
      // Enable after Plan 01 creates src/dashboard/server.ts
    });

    it.skip('starts on custom port when specified', () => {
      // Enable after Plan 01 creates src/dashboard/server.ts
    });

    it.skip('serves index.html at root path', () => {
      // Enable after Plan 01 creates src/dashboard/server.ts
    });

    it.skip('serves /dashboard.js static file mapped to dist/dashboard/app.mjs', () => {
      // Enable after Plan 01: validates the /dashboard.js -> dist/dashboard/app.mjs path alignment
    });
  });

  describe('WebSocket endpoint', () => {
    it.skip('accepts WebSocket upgrade at /ws', () => {
      // Enable after Plan 01 creates src/dashboard/server.ts
    });
  });

  describe('event log tailing', () => {
    it.skip('watches events.log and broadcasts new lines via WebSocket', () => {
      // Enable after Plan 01 creates src/dashboard/server.ts
    });

    it.skip('handles missing events.log file gracefully', () => {
      // Enable after Plan 01 creates src/dashboard/server.ts
    });
  });
});
```

**Create `tests/dashboard/api.test.ts`:**

```typescript
import { describe, it, expect } from 'vitest';

describe('Dashboard API Routes', () => {
  describe('GET /api/status', () => {
    it.skip('returns 200 with node/edge/community counts when bootstrapped', () => {
      // Enable after Plan 01 creates src/dashboard/api/status.ts
    });

    it.skip('returns 404 with NOT_BOOTSTRAPPED code when no database', () => {
      // Enable after Plan 01 creates src/dashboard/api/status.ts
    });
  });

  describe('GET /api/graph', () => {
    it.skip('returns nodes with id, centrality, community, isDangerZone fields', () => {
      // Enable after Plan 01 creates src/dashboard/api/graph.ts
    });

    it.skip('returns edges with source, target, kind fields', () => {
      // Enable after Plan 01 creates src/dashboard/api/graph.ts
    });

    it.skip('returns 404 when database does not exist', () => {
      // Enable after Plan 01 creates src/dashboard/api/graph.ts
    });
  });

  describe('GET /api/conventions', () => {
    it.skip('returns per-file compliance data with color buckets', () => {
      // Enable after Plan 01 creates src/dashboard/api/conventions.ts
    });
  });

  describe('GET /api/readiness', () => {
    it.skip('returns current snapshot with letter grades', () => {
      // Enable after Plan 01 creates src/dashboard/api/readiness.ts
    });

    it.skip('returns history array sorted by timestamp ASC', () => {
      // Enable after Plan 01 creates src/dashboard/api/readiness.ts
    });
  });

  describe('GET /api/blast-radius/:file', () => {
    it.skip('returns rings grouped by hop distance for forward direction', () => {
      // Enable after Plan 01 creates src/dashboard/api/blast-radius.ts
    });

    it.skip('returns reverse blast radius when direction=reverse', () => {
      // Enable after Plan 01 creates src/dashboard/api/blast-radius.ts
    });
  });

  describe('POST /api/review', () => {
    it.skip('accepts file_paths array and returns review result', () => {
      // Enable after Plan 01 creates src/dashboard/api/review.ts
    });
  });

  describe('POST /api/impact', () => {
    it.skip('accepts file_paths array and returns impact prediction', () => {
      // Enable after Plan 01 creates src/dashboard/api/impact.ts
    });
  });
});
```

**Create `tests/dashboard/websocket.test.ts`:**

```typescript
import { describe, it, expect } from 'vitest';

describe('Dashboard WebSocket', () => {
  describe('connection lifecycle', () => {
    it.skip('adds client to broadcast set on connect', () => {
      // Enable after Plan 01 creates src/dashboard/server.ts
    });

    it.skip('removes client from broadcast set on disconnect', () => {
      // Enable after Plan 01 creates src/dashboard/server.ts
    });
  });

  describe('broadcast', () => {
    it.skip('sends JSON stringified event to all connected clients', () => {
      // Enable after Plan 01 creates src/dashboard/server.ts
    });
  });

  describe('event types', () => {
    it('validates WSEvent type discriminator values', () => {
      // Pure logic test -- no implementation dependency
      const validTypes = [
        'bootstrap:progress',
        'orient:phase',
        'agent:spawn',
        'agent:complete',
        'graph:updated',
        'readiness:snapshot',
      ];
      expect(validTypes).toHaveLength(6);
      expect(validTypes).toContain('bootstrap:progress');
      expect(validTypes).toContain('graph:updated');
    });
  });
});
```

All three files should:
- Import only from `vitest` (no implementation imports yet -- those are `it.skip`)
- Have at least one non-skipped test so vitest reports the file as "passed" rather than "skipped entirely"
- The `websocket.test.ts` has one real assertion test for event type validation
  </action>
  <verify>
    <automated>cd /Users/jaywadhwa/codescope && npx vitest run --reporter=verbose tests/dashboard/ 2>&1 | tail -20</automated>
  </verify>
  <done>Three test stub files created in tests/dashboard/. vitest runs them successfully -- skipped tests document the expected structure for Plan 01, one real assertion validates WSEvent type values. Nyquist Wave 0 requirement satisfied.</done>
</task>

</tasks>

<verification>
1. `ls tests/dashboard/*.test.ts` shows 3 files: server.test.ts, api.test.ts, websocket.test.ts
2. `npx vitest run tests/dashboard/` completes with 0 failures
3. Each file contains `describe` and `it.skip` blocks matching VALIDATION.md expectations
</verification>

<success_criteria>
- tests/dashboard/server.test.ts exists with describe blocks for startDashboard, WebSocket endpoint, event log tailing
- tests/dashboard/api.test.ts exists with describe blocks for all 7 API routes
- tests/dashboard/websocket.test.ts exists with describe blocks for connection lifecycle and broadcast
- `npx vitest run tests/dashboard/` passes (skipped tests are acceptable; at least 1 real assertion)
</success_criteria>

<output>
After completion, create `.planning/phases/14-visualization-dashboard/14-00-SUMMARY.md`
</output>
