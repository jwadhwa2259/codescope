---
phase: 16-tech-debt-closure
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [REVIEW-01, REVIEW-02, REVIEW-03, REVIEW-04, IMPACT-01, IMPACT-02, DEBT-02, DIST-03]
gap_closure: true

must_haves:
  truths:
    - "npm install followed by node dist/server.mjs starts without ERR_MODULE_NOT_FOUND"
    - "All 42 v2.0 requirements show Complete in the REQUIREMENTS.md traceability table"
  artifacts:
    - path: "package.json"
      provides: "postinstall script ensuring dist/ is always fresh after install"
      contains: "postinstall"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate traceability table with all 42 requirements marked Complete"
      contains: "Complete"
  key_links:
    - from: "package.json"
      to: "dist/server.mjs"
      via: "postinstall script runs npm run build"
      pattern: "postinstall.*build"
---

<objective>
Close two verification gaps: (1) add a postinstall script to package.json so dist/ is always rebuilt after install, preventing stale ESM import errors, and (2) update 6 traceability table rows in REQUIREMENTS.md from Pending to Complete.

Purpose: Gap 1 ensures users who clone/install the repo get a working dist/ without manual steps. Gap 2 satisfies the "all 42 requirements show Complete in 3-source cross-reference" success criterion.

Output: Updated package.json with postinstall script, updated REQUIREMENTS.md traceability table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-tech-debt-closure/16-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add postinstall build script to package.json</name>
  <files>package.json</files>
  <read_first>
    - package.json (full file -- current scripts section has no postinstall; "build" script is "tsdown"; "prepack" already calls "npm run build")
  </read_first>
  <action>
    Add a `postinstall` script to the `scripts` section of `package.json` that runs the build step. This ensures that after `npm install`, the dist/ directory is always regenerated from the current source, preventing stale ESM imports.

    In the `"scripts"` object, add this entry (after the existing `"prepack"` line):

    ```json
    "postinstall": "npm run build --ignore-scripts",
    ```

    The `--ignore-scripts` flag prevents infinite recursion (postinstall triggering another npm install). The build command is `tsdown` which produces `dist/server.mjs` with the correct ESM imports including `graphology-metrics/centrality/degree.js`.

    Do NOT change any other scripts. Do NOT remove the existing `prepack` script.

    After adding the script, verify it works:
    ```bash
    npm run postinstall
    ```
    Then verify dist/server.mjs contains the fixed import:
    ```bash
    grep "graphology-metrics/centrality/degree" dist/server.mjs
    ```
    It should show the `.js` extension variant (not bare specifier).
  </action>
  <verify>
    <automated>grep -q '"postinstall"' package.json && npm run build 2>&1 | tail -3 && grep -q "graphology-metrics/centrality/degree" dist/server.mjs && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"postinstall": "npm run build --ignore-scripts"` in the scripts section
    - `npm run build` succeeds (exit 0)
    - `dist/server.mjs` exists after build
    - `dist/server.mjs` contains `graphology-metrics/centrality/degree.js` (with .js extension, not bare specifier)
    - The postinstall script does NOT use recursive `npm install`
  </acceptance_criteria>
  <done>package.json has postinstall script that rebuilds dist/ after install. Fresh dist/ contains correct ESM imports.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md traceability table for 6 pending requirements</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file -- traceability table lines 115-158; lines 117-120 show GRAPH-01 through GRAPH-04 as Pending; lines 155-156 show DIST-01, DIST-02 as Pending)
  </read_first>
  <action>
    Update 6 rows in the traceability table at the bottom of REQUIREMENTS.md from "Pending" to "Complete".

    **Exact changes (line numbers refer to current file):**

    Line 117: `| GRAPH-01 | Phase 9 + 16 | Pending |` --> `| GRAPH-01 | Phase 9 + 16 | Complete |`
    Line 118: `| GRAPH-02 | Phase 9 + 16 | Pending |` --> `| GRAPH-02 | Phase 9 + 16 | Complete |`
    Line 119: `| GRAPH-03 | Phase 9 + 16 | Pending |` --> `| GRAPH-03 | Phase 9 + 16 | Complete |`
    Line 120: `| GRAPH-04 | Phase 9 + 16 | Pending |` --> `| GRAPH-04 | Phase 9 + 16 | Complete |`
    Line 155: `| DIST-01 | Phase 15 + 16 | Pending |` --> `| DIST-01 | Phase 15 + 16 | Complete |`
    Line 156: `| DIST-02 | Phase 15 + 16 | Pending |` --> `| DIST-02 | Phase 15 + 16 | Complete |`

    Also update the coverage summary near line 164:
    - Change: `Pending (Phase 16 gap closure): 15`
    - To: `Pending: 0`

    And update the last-updated timestamp:
    - Change: `*Last updated: 2026-03-29 after gap closure phase creation (15 requirements assigned to Phase 16)*`
    - To: `*Last updated: 2026-03-29 after gap closure -- all 42 requirements Complete*`

    Do NOT change any other content in the file. The [x] checkboxes in the requirements section above are already correct.
  </action>
  <verify>
    <automated>grep -c "Pending" .planning/REQUIREMENTS.md | xargs -I{} sh -c 'if [ {} -ne 0 ]; then echo "FAIL: {} rows still Pending"; exit 1; else echo "PASS: zero Pending rows"; fi'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` line for GRAPH-01 shows `| GRAPH-01 | Phase 9 + 16 | Complete |`
    - `.planning/REQUIREMENTS.md` line for GRAPH-02 shows `| GRAPH-02 | Phase 9 + 16 | Complete |`
    - `.planning/REQUIREMENTS.md` line for GRAPH-03 shows `| GRAPH-03 | Phase 9 + 16 | Complete |`
    - `.planning/REQUIREMENTS.md` line for GRAPH-04 shows `| GRAPH-04 | Phase 9 + 16 | Complete |`
    - `.planning/REQUIREMENTS.md` line for DIST-01 shows `| DIST-01 | Phase 15 + 16 | Complete |`
    - `.planning/REQUIREMENTS.md` line for DIST-02 shows `| DIST-02 | Phase 15 + 16 | Complete |`
    - Zero occurrences of `| Pending |` in the traceability table
    - Coverage summary says `Pending: 0`
  </acceptance_criteria>
  <done>All 42 v2.0 requirements show Complete in the traceability table. Zero Pending rows remain.</done>
</task>

</tasks>

<verification>
1. `grep -c "Pending" .planning/REQUIREMENTS.md` returns 0
2. `grep '"postinstall"' package.json` shows the postinstall script
3. `npm run build && node dist/server.mjs &; sleep 2; kill %1 2>/dev/null` -- no ERR_MODULE_NOT_FOUND in output
</verification>

<success_criteria>
- package.json has postinstall script that runs build
- dist/ is rebuilt with correct ESM imports after running postinstall
- All 42 requirements show Complete in traceability table
- Zero Pending rows in REQUIREMENTS.md
</success_criteria>

<output>
After completion, create `.planning/phases/16-tech-debt-closure/16-04-SUMMARY.md`
</output>
