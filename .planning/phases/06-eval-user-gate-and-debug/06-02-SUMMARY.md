---
phase: 06-eval-user-gate-and-debug
plan: 02
subsystem: debug
tags: [debug, fix-planner, design-decision, scoped-re-eval, execution-plan, bounded-loop]

# Dependency graph
requires:
  - phase: 06-eval-user-gate-and-debug
    provides: EvalFinding, EvalResult, DebugCycleResult types, parseEvalFindings, appendDebugCycleSection
  - phase: 04-orient-and-execution-engine
    provides: ExecutionPlan, AgentAssignment, execution orchestrator pattern
provides:
  - DebugOptions, DebugResult, DebugCallbacks, DesignDecision, FixTask types
  - Fix planner creating ExecutionPlan-compatible mini plans from findings grouped by file
  - Debug agent bounded loop with scoped re-verify and re-eval
  - Design decision escalation via callback
affects: [06-03, 06-04]

# Tech tracking
tech-stack:
  added: []
  patterns: [debug agent module pattern (Options + Result + Callbacks + async function), finding match key for cross-ID resolution tracking, bounded fix loop with early termination]

key-files:
  created:
    - src/debug/types.ts
    - src/debug/fix-planner.ts
    - src/debug/debug-agent.ts
    - tests/debug/fix-planner.test.ts
    - tests/debug/debug-agent.test.ts
  modified: []

key-decisions:
  - "Finding resolution uses file+criterion+line-bucket match keys instead of ID comparison, since re-eval findings get fresh IDs from parseEvalFindings"
  - "Design decision escalation uses 3 fixed options (fix, alternative, skip) -- nuanced options generated by LLM at runtime"
  - "Crash recovery is handled at the per-agent dispatch level with try/catch, preserving committed fixes per D-27"

patterns-established:
  - "Debug module pattern: types.ts + fix-planner.ts + debug-agent.ts matching eval module structure"
  - "Finding match key: criterion + sanitized-file + 5-line-bucket for cross-cycle resolution tracking"
  - "Bounded loop with hasActionableFindings early termination for INFO-only finding sets"

requirements-completed: [DBUG-01, DBUG-02, DBUG-03, DBUG-04, DBUG-05, DBUG-06, DBUG-07]

# Metrics
duration: 5min
completed: 2026-03-24
---

# Phase 6 Plan 02: Debug Agent Summary

**Debug agent with bounded fix loop creating ExecutionPlan-compatible mini plans from eval findings, design decision escalation, scoped re-verify/re-eval, and crash-safe resolution tracking**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-24T18:11:33Z
- **Completed:** 2026-03-24T18:16:57Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Full debug type system: DebugOptions, DebugResult, DebugCallbacks, DesignDecision, FixTask
- Fix planner groups findings by file, creates mini ExecutionPlan objects capped at 3 agents per D-11
- Debug agent runs bounded fix cycles with scoped re-verify and scoped re-eval per D-13
- Design decisions detected by category and escalated via callback with 3 options per D-12/D-29
- Max cycles enforcement with status report per D-14, early termination for INFO-only per D-20
- Crash recovery preserves committed fixes per D-27 with try/catch around fix dispatch

## Task Commits

Each task was committed atomically:

1. **Task 1: Debug types and fix planner** - `0f0fba0` (test: RED), `ce0653c` (feat: GREEN)
2. **Task 2: Debug agent loop** - `ef0d201` (test: RED), `cab007b` (feat: GREEN)

_TDD tasks have RED (failing test) and GREEN (implementation) commits._

## Files Created/Modified
- `src/debug/types.ts` - DebugOptions, DebugResult, DebugCallbacks, DesignDecision, FixTask types
- `src/debug/fix-planner.ts` - createFixPlan (groups by file, caps at 3 agents), isDesignDecision, buildFixPrompt
- `src/debug/debug-agent.ts` - runDebug bounded loop with dispatch, re-verify, re-eval, resolution tracking
- `tests/debug/fix-planner.test.ts` - 12 tests for fix planner functions
- `tests/debug/debug-agent.test.ts` - 12 tests for debug agent loop

## Decisions Made
- Finding resolution uses file+criterion+line-bucket match keys instead of ID comparison -- re-eval generates fresh IDs via parseEvalFindings so direct ID comparison would fail
- Design decision escalation uses 3 fixed structural options; the LLM generates nuanced descriptions at runtime
- Crash recovery wraps each agent dispatch in try/catch; committed fixes survive because they are already persisted by the fix agent before crash

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed finding resolution matching logic**
- **Found during:** Task 2 (debug agent loop implementation)
- **Issue:** Re-eval findings get fresh IDs from parseEvalFindings using the eval-{criterion}-{file}-{bucket} format, which never matches original finding IDs. This caused all findings to appear "resolved" after every cycle.
- **Fix:** Introduced findingMatchKey helper using criterion+sanitized-file+line-bucket for stable cross-cycle matching
- **Files modified:** src/debug/debug-agent.ts
- **Verification:** Tests confirm correct resolution tracking across cycles
- **Committed in:** cab007b (Task 2 GREEN commit)

---

**Total deviations:** 1 auto-fixed (1 bug fix)
**Impact on plan:** Bug fix essential for correct resolution tracking. No scope creep.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Debug types ready for CLI entry point (Plan 03) and MCP tool integration (Plan 04)
- Fix planner produces ExecutionPlan-compatible plans for orchestrator dispatch
- Debug agent ready for skill body integration (Step 6: eval -> gate -> debug loop)
- All 24 tests passing across debug test suite

## Self-Check: PASSED

All 5 created files verified on disk. All 4 task commits (0f0fba0, ce0653c, ef0d201, cab007b) verified in git log. 24/24 tests pass.

---
*Phase: 06-eval-user-gate-and-debug*
*Completed: 2026-03-24*
