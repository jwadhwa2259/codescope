---
phase: 03-bootstrap-synthesis-and-mcp-server
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/tools/recall.ts
  - src/tools/conventions.ts
  - src/tools/readiness-tool.ts
  - src/tools/service-map.ts
  - tests/tools/recall.test.ts
  - tests/tools/conventions.test.ts
  - tests/tools/readiness-tool.test.ts
  - tests/tools/service-map.test.ts
autonomous: true
requirements: [MCP-02, MCP-05, MCP-09, MCP-12]

must_haves:
  truths:
    - "codescope_recall returns combined context from overview.md, conventions.md, and learnings.md filtered by topic"
    - "codescope_conventions returns conventions filtered by file path or module name with adoption percentages"
    - "codescope_readiness returns structured AI readiness score data from readiness.md"
    - "codescope_service_map returns service list with dependencies and shared types"
    - "All four tools return NOT_BOOTSTRAPPED error when no bootstrap data exists"
    - "All four tools include staleness metadata per D-18"
    - "Single-service projects get a one-service response with empty dependencies array per D-34"
  artifacts:
    - path: "src/tools/recall.ts"
      provides: "codescope_recall MCP tool implementation"
      exports: ["registerRecallTool"]
    - path: "src/tools/conventions.ts"
      provides: "codescope_conventions MCP tool implementation"
      exports: ["registerConventionsTool"]
    - path: "src/tools/readiness-tool.ts"
      provides: "codescope_readiness MCP tool implementation"
      exports: ["registerReadinessTool"]
    - path: "src/tools/service-map.ts"
      provides: "codescope_service_map MCP tool implementation"
      exports: ["registerServiceMapTool"]
  key_links:
    - from: "src/tools/recall.ts"
      to: ".claude/codescope/"
      via: "reads overview.md, conventions.md, learnings.md"
      pattern: "fs\\.readFileSync.*overview\\.md|conventions\\.md|learnings\\.md"
    - from: "src/tools/service-map.ts"
      to: ".claude/codescope/"
      via: "reads service-manifest.md, cross-service-map.md"
      pattern: "service-manifest\\.md|cross-service-map\\.md"
---

<objective>
Implement 4 file-reading MCP tools that serve codebase intelligence by reading markdown artifacts produced by bootstrap agents. These tools do not require graph access -- they parse and filter markdown files.

Purpose: Agents calling codescope_recall, codescope_conventions, codescope_readiness, and codescope_service_map get structured intelligence without needing to read raw markdown files themselves.
Output: 4 new tool modules + 4 test files.
</objective>

<execution_context>
@/Users/jaywadhwa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaywadhwa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bootstrap-synthesis-and-mcp-server/03-CONTEXT.md
@.planning/phases/03-bootstrap-synthesis-and-mcp-server/03-RESEARCH.md
@.planning/phases/03-bootstrap-synthesis-and-mcp-server/03-UI-SPEC.md
@.planning/phases/03-bootstrap-synthesis-and-mcp-server/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (helpers.ts, meta.ts) -->

From src/tools/helpers.ts:
```typescript
export type Staleness = "fresh" | "stale" | "very_stale";
export interface ToolMetadata {
  last_bootstrap: string | null;
  staleness: Staleness;
  query_ms: number;
  capabilities?: string[];
  upcoming?: string[];
}
export function okResponse(data: unknown, metadata: ToolMetadata): { content: Array<{ type: "text"; text: string }> };
export function errorResponse(code: string, message: string, recovery: string): { content: Array<{ type: "text"; text: string }> };
export function partialResponse(data: unknown, warnings: string[], metadata: ToolMetadata): { content: Array<{ type: "text"; text: string }> };
export function isBootstrapped(projectRoot: string): boolean;
export function buildMetadata(projectRoot: string, startMs: number, extras?: { capabilities?: string[]; upcoming?: string[] }): ToolMetadata;
```

From src/tools/stubs.ts (Zod schemas to reuse/enhance):
```typescript
// codescope_recall
{ topic: z.string().describe("Topic to recall information about") }
// codescope_conventions
{ file_path: z.string().optional(), module: z.string().optional() }
// codescope_readiness
{}
// codescope_service_map
{}
```

From src/utils/paths.ts:
```typescript
export function getCodescopePath(projectRoot: string): string;
```

From src/tools/status.ts (registration pattern):
```typescript
export function registerStatusTool(server: McpServer, projectRoot: string): void {
  server.tool("codescope_status", "description", { ...schema }, async ({ ...params }) => { ... });
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: codescope_recall and codescope_conventions tools</name>
  <files>src/tools/recall.ts, src/tools/conventions.ts, tests/tools/recall.test.ts, tests/tools/conventions.test.ts</files>
  <read_first>
    - src/tools/status.ts (registration pattern to follow)
    - src/tools/helpers.ts (response builders from Plan 01)
    - src/tools/stubs.ts (existing Zod schemas for recall and conventions)
    - src/utils/paths.ts (getCodescopePath)
    - .planning/phases/03-bootstrap-synthesis-and-mcp-server/03-CONTEXT.md (D-20 recall behavior, D-35 rich descriptions)
    - .planning/phases/03-bootstrap-synthesis-and-mcp-server/03-UI-SPEC.md (error codes, empty state copy)
  </read_first>
  <behavior>
    Recall tests (tests/tools/recall.test.ts):
    - Test 1: Returns combined context from overview.md, conventions.md, learnings.md when all exist
    - Test 2: Returns partial result with warnings when some artifacts missing
    - Test 3: Filters sections by topic keyword (case-insensitive match on H2 headings and content)
    - Test 4: Returns NOT_BOOTSTRAPPED error when graph.db does not exist
    - Test 5: Response follows D-17 format with staleness metadata
    - Test 6: Returns empty data with note when topic matches nothing

    Conventions tests (tests/tools/conventions.test.ts):
    - Test 7: Returns all conventions when no filter specified
    - Test 8: Filters conventions by file_path (matches conventions whose files list includes the path)
    - Test 9: Filters conventions by module name
    - Test 10: Returns NOT_BOOTSTRAPPED error when not bootstrapped
    - Test 11: Returns empty conventions message when conventions.md has no entries
    - Test 12: Response includes adoption percentages and confidence levels
  </behavior>
  <action>
    Create `src/tools/recall.ts`:
    - Export `function registerRecallTool(server: McpServer, projectRoot: string): void`
    - Register tool `"codescope_recall"` with enhanced description per D-35: "Retrieve conventions, learnings, and codebase overview for a topic. Returns combined context from overview.md, conventions.md, and learnings.md filtered by topic keyword. Use this before making changes to understand the codebase context. Related tools: codescope_conventions, codescope_orient."
    - Input schema: `{ topic: z.string().describe("Topic to recall information about (e.g., 'authentication', 'error handling', 'imports')") }`
    - Handler logic per D-20:
      1. Check `isBootstrapped(projectRoot)` -- return errorResponse("NOT_BOOTSTRAPPED", ...) if false
      2. Read 3 files from `getCodescopePath(projectRoot)`: `overview.md`, `conventions.md`, `learnings.md`
      3. For each file that exists, extract sections (split by `## ` headings)
      4. Filter sections: include section if heading or body contains `topic` (case-insensitive)
      5. If no filters match, include a summary section from each file (first 20 lines)
      6. Return okResponse with `{ overview: string, conventions: string, learnings: string }` and metadata
      7. If some files missing, use partialResponse with warnings listing missing files

    Create `src/tools/conventions.ts`:
    - Export `function registerConventionsTool(server: McpServer, projectRoot: string): void`
    - Register tool `"codescope_conventions"` with enhanced description per D-35: "Get detected coding conventions for specific files or modules. Returns convention patterns with adoption percentages, confidence levels, and evidence. Use before writing code to follow established patterns. Related tools: codescope_recall, codescope_verify."
    - Input schema: `{ file_path: z.string().optional().describe("File path to check conventions for"), module: z.string().optional().describe("Module name to check conventions for") }`
    - Handler logic:
      1. Check isBootstrapped -- return NOT_BOOTSTRAPPED error if false
      2. Read `conventions.md` from codescope path
      3. Parse conventions: each convention is a section with name, adoption %, confidence, evidence lines, applicable files
      4. If `file_path` provided, filter to conventions whose evidence files or detected files include the path
      5. If `module` provided, filter to conventions whose category or name matches the module
      6. If neither provided, return all conventions
      7. Return okResponse with `{ conventions: Array<{ name, adoption_pct, confidence, category, evidence: string[] }>, total: number, filtered: number }`
      8. Include empty state message per UI-SPEC if no conventions found: "No conventions detected. This may indicate a very small codebase or highly varied coding patterns."
  </action>
  <verify>
    <automated>npx vitest run tests/tools/recall.test.ts tests/tools/conventions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/tools/recall.ts exports `registerRecallTool`
    - src/tools/recall.ts contains `"codescope_recall"` tool registration
    - src/tools/recall.ts contains `isBootstrapped(projectRoot)` check
    - src/tools/recall.ts reads `overview.md`, `conventions.md`, `learnings.md`
    - src/tools/recall.ts contains `okResponse(` and `errorResponse(`
    - src/tools/conventions.ts exports `registerConventionsTool`
    - src/tools/conventions.ts contains `"codescope_conventions"` tool registration
    - src/tools/conventions.ts contains filtering logic for `file_path` and `module`
    - tests/tools/recall.test.ts contains at least 5 test cases
    - tests/tools/conventions.test.ts contains at least 5 test cases
    - `npx vitest run tests/tools/recall.test.ts tests/tools/conventions.test.ts` exits 0
  </acceptance_criteria>
  <done>codescope_recall returns combined topic-filtered context from 3 markdown artifacts. codescope_conventions returns filtered conventions with adoption data. Both handle not-bootstrapped and empty states. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: codescope_readiness and codescope_service_map tools</name>
  <files>src/tools/readiness-tool.ts, src/tools/service-map.ts, tests/tools/readiness-tool.test.ts, tests/tools/service-map.test.ts</files>
  <read_first>
    - src/tools/status.ts (registration pattern)
    - src/tools/helpers.ts (response builders from Plan 01)
    - src/tools/stubs.ts (existing Zod schemas for readiness and service_map)
    - src/utils/paths.ts (getCodescopePath)
    - .planning/phases/03-bootstrap-synthesis-and-mcp-server/03-CONTEXT.md (D-33, D-34 service map; readiness score)
    - .planning/phases/03-bootstrap-synthesis-and-mcp-server/03-UI-SPEC.md (readiness.md format, cross-service-map.md format)
  </read_first>
  <behavior>
    Readiness tool tests (tests/tools/readiness-tool.test.ts):
    - Test 1: Returns structured readiness score from readiness.md (overall grade, percent, dimensions)
    - Test 2: Returns NOT_BOOTSTRAPPED error when not bootstrapped
    - Test 3: Returns error when readiness.md does not exist (bootstrap ran but readiness not computed)
    - Test 4: Parses dimension scores including delta values
    - Test 5: Response includes improvements array
    - Test 6: Response follows D-17 format with metadata

    Service map tests (tests/tools/service-map.test.ts):
    - Test 7: Returns service list with LOC, framework, analysis status from service-manifest.md
    - Test 8: Returns dependency edges from cross-service-map.md (shared types, import counts)
    - Test 9: Single-service project returns one-service response with empty dependencies per D-34
    - Test 10: Returns NOT_BOOTSTRAPPED error when not bootstrapped
    - Test 11: Returns partial response when cross-service-map.md missing but service-manifest.md exists
    - Test 12: Response follows D-17 format
  </behavior>
  <action>
    Create `src/tools/readiness-tool.ts` (named readiness-tool to avoid conflict with src/bootstrap/readiness.ts):
    - Export `function registerReadinessTool(server: McpServer, projectRoot: string): void`
    - Register tool `"codescope_readiness"` with enhanced description per D-35: "Get the AI readiness score for this codebase. Returns a letter grade (A-F) with per-dimension breakdown (convention coverage, type safety, test coverage proxy, import graph health), delta tracking from previous bootstrap, and top 3 improvement suggestions. Related tools: codescope_status, codescope_conventions."
    - Input schema: `{}` (no parameters)
    - Handler logic:
      1. Check isBootstrapped -- return NOT_BOOTSTRAPPED error if false
      2. Read `readiness.md` from codescope path
      3. Parse the markdown: extract overall grade/percent from H1, dimension table rows, improvements list
      4. Return okResponse with `{ overall: { grade, percent }, dimensions: { convention_coverage: { percent, grade, delta, explainer }, type_safety: {...}, test_coverage_proxy: {...}, import_graph_health: {...} }, improvements: string[] }`
      5. If readiness.md missing, return errorResponse("NODE_NOT_FOUND", "Readiness score not yet computed. Run /codescope:bootstrap first.", "Run /codescope:bootstrap to generate readiness assessment.")

    Create `src/tools/service-map.ts`:
    - Export `function registerServiceMapTool(server: McpServer, projectRoot: string): void`
    - Register tool `"codescope_service_map"` with enhanced description per D-35: "Get the cross-service dependency map for this project. Returns service list with LOC, frameworks, and analysis status, plus dependency edges with shared types and import counts. For single-service projects, returns one service with empty dependencies. Related tools: codescope_graph_query, codescope_blast_radius."
    - Input schema: `{}` (no parameters)
    - Handler logic per D-33, D-34:
      1. Check isBootstrapped -- return NOT_BOOTSTRAPPED error if false
      2. Read `service-manifest.md` from codescope path
      3. Parse service table rows: extract name, path, LOC, framework, analysis status
      4. Read `cross-service-map.md` from codescope path (may not exist for single-service)
      5. If cross-service-map exists, parse dependency table rows: from, to, shared_types, import_count
      6. If not exists or single-service, return empty dependencies array per D-34
      7. Return okResponse with `{ services: Array<{ name, path, loc, framework, analysis }>, dependencies: Array<{ from, to, shared_types: string[], import_count: number }> }`
      8. If service-manifest exists but cross-service-map missing, use partialResponse with warning
  </action>
  <verify>
    <automated>npx vitest run tests/tools/readiness-tool.test.ts tests/tools/service-map.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/tools/readiness-tool.ts exports `registerReadinessTool`
    - src/tools/readiness-tool.ts contains `"codescope_readiness"` tool registration
    - src/tools/readiness-tool.ts reads and parses `readiness.md`
    - src/tools/service-map.ts exports `registerServiceMapTool`
    - src/tools/service-map.ts contains `"codescope_service_map"` tool registration
    - src/tools/service-map.ts reads `service-manifest.md` and `cross-service-map.md`
    - src/tools/service-map.ts handles single-service case with empty dependencies array
    - tests/tools/readiness-tool.test.ts contains at least 5 test cases
    - tests/tools/service-map.test.ts contains at least 5 test cases
    - `npx vitest run tests/tools/readiness-tool.test.ts tests/tools/service-map.test.ts` exits 0
  </acceptance_criteria>
  <done>codescope_readiness returns structured score data from readiness.md. codescope_service_map returns service list with cross-service dependencies. Both handle missing artifacts and not-bootstrapped states. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/tools/recall.test.ts tests/tools/conventions.test.ts tests/tools/readiness-tool.test.ts tests/tools/service-map.test.ts` -- all pass
- `npx vitest run` -- full suite green
- Each tool file contains `isBootstrapped(projectRoot)` guard
- Each tool file contains `okResponse(` or `errorResponse(`
- Each tool file has enhanced description per D-35
</verification>

<success_criteria>
- codescope_recall combines 3 artifact files filtered by topic per D-20 (MCP-02)
- codescope_conventions filters by file/module with adoption data per MCP-05
- codescope_readiness returns structured score per MCP-09
- codescope_service_map handles both monorepo and single-service per D-33/D-34 (MCP-12)
- All tools follow D-17 response contract with staleness metadata
- Full test suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/03-bootstrap-synthesis-and-mcp-server/03-02-SUMMARY.md`
</output>
