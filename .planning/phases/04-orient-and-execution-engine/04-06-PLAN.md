---
phase: 04-orient-and-execution-engine
plan: 06
type: execute
wave: 3
depends_on: ["04-04", "04-05"]
files_modified:
  - src/orient/pipeline.ts
  - src/orient/run-orient.ts
  - src/execution/run-execution.ts
  - skills/orient/SKILL.md
  - tests/orient/pipeline.test.ts
autonomous: false
requirements: [ORNT-01, ORNT-11, EXEC-01, EXEC-04, EXEC-08, EXEC-09]
must_haves:
  truths:
    - "/codescope:orient [task] triggers the full autonomous pipeline from clarification through execution"
    - "Pipeline orchestrates: clarification -> Gate 1 -> research -> analysis -> planning -> validation -> Gate 2 -> execution"
    - "--no-confirm skips both gates, --no-clarify skips clarification"
    - "Research and planning run as sub-agents via Task/Agent tool invocation"
    - "Execution agents are dispatched per the orchestrator's wave schedule"
    - "Agent teams detection at runtime enables parallel wave execution or falls back to sequential"
    - "Orient completes research + analysis + planning + validation in under 60 seconds (excluding clarification and execution)"
    - "All artifacts persisted to disk regardless of outcome (scope contract, plan, coordination, summary)"
  artifacts:
    - path: "src/orient/pipeline.ts"
      provides: "Full orient pipeline orchestration: clarification through plan approval"
      exports: ["runOrientPipeline", "PipelineOptions", "PipelineResult"]
    - path: "src/orient/run-orient.ts"
      provides: "CLI entry point for orient pipeline (called by skill via node --import tsx/esm)"
      min_lines: 30
    - path: "src/execution/run-execution.ts"
      provides: "CLI entry point for execution engine (called by skill via node --import tsx/esm)"
      min_lines: 30
    - path: "skills/orient/SKILL.md"
      provides: "Full orient skill body replacing stub"
      contains: "codescope:orient"
  key_links:
    - from: "skills/orient/SKILL.md"
      to: "src/orient/run-orient.ts"
      via: "node --import tsx/esm src/orient/run-orient.ts --phase {phase}"
      pattern: "run-orient.*--phase"
    - from: "skills/orient/SKILL.md"
      to: "src/execution/run-execution.ts"
      via: "node --import tsx/esm src/execution/run-execution.ts"
      pattern: "run-execution"
    - from: "src/orient/pipeline.ts"
      to: "src/orient/clarification.ts"
      via: "runClarification for ambiguity detection"
      pattern: "runClarification"
    - from: "src/orient/pipeline.ts"
      to: "src/orient/analysis.ts"
      via: "runAnalysis for graph traversal"
      pattern: "runAnalysis"
    - from: "src/orient/pipeline.ts"
      to: "src/orient/planner.ts"
      via: "runPlanner for plan generation"
      pattern: "runPlanner"
    - from: "src/orient/pipeline.ts"
      to: "src/orient/validation.ts"
      via: "validatePlan + autoFixPlan"
      pattern: "validatePlan|autoFixPlan"
---

<objective>
Wire together the full orient pipeline, CLI entry points, and the orient skill body that replaces the stub.

Purpose: This is the integration layer that connects clarification, research, analysis, planning, validation, and execution into a single end-to-end flow triggered by `/codescope:orient [task]`. The orient skill body (SKILL.md) is the user-facing entry point that Claude executes conversationally, with two gates for scope and plan approval. This plan ties all the work from Plans 01-05 together.

Output: src/orient/pipeline.ts, src/orient/run-orient.ts, src/execution/run-execution.ts, skills/orient/SKILL.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-orient-and-execution-engine/04-CONTEXT.md
@.planning/phases/04-orient-and-execution-engine/04-RESEARCH.md
@.planning/phases/04-orient-and-execution-engine/04-UI-SPEC.md

@.planning/phases/04-orient-and-execution-engine/04-01-SUMMARY.md
@.planning/phases/04-orient-and-execution-engine/04-04-SUMMARY.md
@.planning/phases/04-orient-and-execution-engine/04-02-SUMMARY.md
@.planning/phases/04-orient-and-execution-engine/04-05-SUMMARY.md

<interfaces>
From src/orient/clarification.ts (Plan 01):
```typescript
export function runClarification(options: ClarificationOptions): Promise<ClarificationResult>;
export function buildScopeContract(...): ScopeContract;
export function writeScopeContractArtifact(contract: ScopeContract, outputDir: string): string;
```

From src/orient/analysis.ts (Plan 01):
```typescript
export function runAnalysis(options: AnalysisOptions): Promise<AnalysisResult>;
export function writeAnalysisArtifact(result: AnalysisResult, outputDir: string): string;
```

From src/orient/research.ts (Plan 04):
```typescript
export function runResearch(options: ResearchOptions): Promise<ResearchOutput>;
export function buildResearchPrompt(...): string;
export function writeResearchArtifact(output: ResearchOutput, outputDir: string): string;
```

From src/orient/planner.ts (Plan 04):
```typescript
export function runPlanner(options: PlannerOptions): Promise<{ prompt: string; plan: ExecutionPlan | null }>;
export function buildPlannerPrompt(options: PlannerOptions): string;
export function parsePlanOutput(content: string): ExecutionPlan;
export function writePlanArtifact(plan: ExecutionPlan, outputDir: string): string;
```

From src/orient/validation.ts (Plan 04):
```typescript
export function validatePlan(plan: ExecutionPlan, scopeContract: ScopeContract): ValidationResult;
export function autoFixPlan(plan: ExecutionPlan, result: ValidationResult, maxAttempts?: number): { plan: ExecutionPlan; result: ValidationResult };
```

From src/orient/types.ts (Plan 01):
```typescript
export interface PipelineOptions { projectRoot: string; task: string; taskSlug: string; noConfirm?: boolean; noClarify?: boolean; onProgress?: (msg: string) => void; onGate?: (gate: 'scope' | 'plan', artifact: string) => Promise<'approve' | 'edit' | 'reject'> }
export interface PipelineResult { status: 'approved' | 'rejected' | 'error'; scopeContractPath: string | null; planPath: string | null; executionDir: string; error?: string }
```

From src/execution/orchestrator.ts (Plan 05):
```typescript
export function runExecution(options: ExecutionOptions): Promise<ExecutionResult>;
export interface ExecutionCallbacks { dispatchAgent: (invocation: AgentInvocation) => Promise<{ success: boolean; output?: string; error?: string }>; onProgress: (message: string) => void }
```

From src/execution/teams-detector.ts (Plan 02):
```typescript
export function detectAgentTeams(): TeamsAvailability;
```

From skills/bootstrap/SKILL.md (existing pattern):
Skill body runs `node --import tsx/esm src/bootstrap/run-bootstrap.ts` via Bash tool.

From src/config/loader.ts:
```typescript
export function loadConfig(projectRoot: string): Config;
```

From src/utils/paths.ts:
```typescript
export function getCodescopePath(projectRoot: string): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Orient pipeline orchestrator and CLI entry points</name>
  <files>src/orient/pipeline.ts, src/orient/run-orient.ts, src/execution/run-execution.ts, tests/orient/pipeline.test.ts</files>
  <read_first>
    - src/orient/types.ts (PipelineOptions, PipelineResult)
    - src/orient/clarification.ts (runClarification, writeScopeContractArtifact exports)
    - src/orient/analysis.ts (runAnalysis, writeAnalysisArtifact exports)
    - src/orient/research.ts (runResearch, buildResearchPrompt, writeResearchArtifact exports)
    - src/orient/planner.ts (runPlanner, buildPlannerPrompt, parsePlanOutput, writePlanArtifact exports)
    - src/orient/validation.ts (validatePlan, autoFixPlan exports)
    - src/execution/orchestrator.ts (runExecution, ExecutionCallbacks exports)
    - src/execution/teams-detector.ts (detectAgentTeams export)
    - src/config/loader.ts (loadConfig)
    - src/utils/paths.ts (getCodescopePath)
    - src/bootstrap/orchestrator.ts (runBootstrap pattern -- Options + Result + sequential steps)
    - .planning/phases/04-orient-and-execution-engine/04-CONTEXT.md lines 16-18 (D-01 two-gate pipeline, D-02 --no-confirm)
    - .planning/phases/04-orient-and-execution-engine/04-RESEARCH.md lines 188-223 (pipeline pattern)
    - .planning/phases/04-orient-and-execution-engine/04-RESEARCH.md lines 684-691 (skill-as-orchestrator pattern)
    - .planning/phases/04-orient-and-execution-engine/04-UI-SPEC.md lines 82-98 (gate copy)
    - .planning/phases/04-orient-and-execution-engine/04-UI-SPEC.md lines 110-151 (progress and error messages)
    - .planning/phases/04-orient-and-execution-engine/04-RESEARCH.md lines 384-394 (task slug collision, 60s budget)
  </read_first>
  <action>
    **src/orient/pipeline.ts**: Full orient pipeline orchestration.

    Export `slugifyTask(task: string): string`:
    - Convert task to lowercase, replace non-alphanumeric with hyphens, collapse multiple hyphens, trim hyphens
    - Append short timestamp suffix to prevent collisions per Pitfall 7: `${slug}-${Date.now().toString(36).slice(-6)}`
    - Cap at 60 characters

    Export `runOrientPipeline(options: PipelineOptions): Promise<PipelineResult>`:
    - **Setup**: Create execution directory at `.claude/codescope/execution/{taskSlug}/`. Create plans directory at `.claude/codescope/plans/` if not exists.
    - **Step 1: Clarification** (per D-01):
      - Emit progress: `"## Clarifying scope..."`
      - Call `runClarification({ projectRoot, task, taskSlug, clarificationStyle: config.orient.clarification, outputDir: executionDir, noClarify: options.noClarify })`
      - If needsClarification: return the questions for the skill body to present to user (the pipeline pauses here -- skill body handles the conversation)
      - If not: auto-generate scope contract from analysis
    - **Step 2: Gate 1 - Scope Approval** (per D-01, D-08):
      - Write scope contract artifact via `writeScopeContractArtifact()`
      - If not noConfirm: call `options.onGate('scope', scopeContractPath)` and wait for response
      - If 'reject': return to clarification with rejection reason per D-09 (set status rejected, include error)
      - If 'edit': update scope contract with edits and re-present
      - If 'approve': continue. Set scope contract status to 'APPROVED'.
    - **Step 3: Research** (per D-10):
      - Emit progress: `"## Researching..."`
      - Call `runResearch({ projectRoot, taskSlug, task, analysisResult, scopeContract, outputDir: executionDir })`
      - This prepares the research prompt. The actual sub-agent execution is handled by the skill body (it runs the Task/Agent tool).
      - Write research artifact
    - **Step 4: Analysis** (per ORNT-08):
      - This runs as a direct module call (not a sub-agent -- graph operations are fast)
      - Extract keywords from task
      - Call `runAnalysis({ projectRoot, taskSlug, keywords, outputDir: executionDir })`
      - Write analysis artifact
    - **Step 5: Planning** (per ORNT-09):
      - Emit progress: `"## Planning..."`
      - Call `runPlanner({ projectRoot, taskSlug, task, scopeContract, analysisResult, researchOutput })`
      - This prepares the planner prompt. Actual sub-agent execution handled by skill body.
      - Parse plan output and write plan artifact to `plans/{taskSlug}.md` per ORNT-10
    - **Step 6: Validation** (per D-19):
      - Emit progress: `"## Validating plan..."`
      - Call `validatePlan(plan, scopeContract)`
      - If mechanical errors: call `autoFixPlan(plan, result)` per D-20
      - Write validation section into plan artifact per D-22
    - **Step 7: Gate 2 - Plan Approval** (per D-14, D-15):
      - If not noConfirm: call `options.onGate('plan', planPath)` and wait
      - If 'reject': return to scope per D-15
      - If 'edit': apply edits, re-validate, re-present
      - If 'approve': set plan status to 'APPROVED'
    - **Return** PipelineResult with paths and status

    NOTE on execution: The pipeline does NOT call runExecution directly. It returns the approved plan path. The skill body then calls run-execution.ts separately. This keeps the pipeline focused on orient (research + planning) and the execution engine separate.

    **Budget tracking for ORNT-11**: The pipeline tracks cumulative time for steps 3-6 (research + analysis + planning + validation). If approaching 60s budget, emit a warning but don't hard-fail.

    **src/orient/run-orient.ts**: CLI entry point called by the skill body via `node --import tsx/esm src/orient/run-orient.ts`.

    - Parse CLI args: `--project-root`, `--task`, `--phase`, `--task-slug`, `--answers`, `--no-confirm`, `--no-clarify`, `--check-only`
    - The `--phase` flag controls which pipeline step(s) to run. Valid values:
      - `clarification` -- runs only the clarification step (ambiguity detection + question generation). Returns JSON with `needsClarification`, `questions`, `taskSlug`, and `outputDir`. Used by skill body Step 1.
      - `scope-contract` -- runs scope contract generation using user's answers (passed via `--answers` as JSON string). Requires `--task-slug`. Returns JSON with `scopeContractPath`.
      - `research` -- runs research topic extraction and prompt preparation. Requires `--task-slug`. Returns JSON with `researchPrompt`, `topics`, and `outputDir`.
      - `analysis-and-planning` -- runs analysis (graph traversal) then planner prompt preparation. Requires `--task-slug`. Returns JSON with `analysisResult`, `plannerPrompt`, `planPath`, and `validationResult`.
    - When `--phase` is omitted: run the full pipeline non-interactively (equivalent to `--no-confirm --no-clarify`). Useful for automation.
    - When `--check-only` is passed: verify bootstrap exists (isBootstrapped check), output `{ bootstrapped: true/false }`, exit.
    - Generate taskSlug via `slugifyTask(task)` unless `--task-slug` is provided (phases after clarification reuse the slug).
    - Output result as JSON to stdout for the skill body to parse
    - Exit 0 on success, 1 on error
    - NOTE: The onGate callback is NOT implemented here (gates are conversational -- handled by the skill body). run-orient.ts runs the non-interactive parts only.

    **src/execution/run-execution.ts**: CLI entry point for execution.

    - Parse CLI args: `--project-root`, `--task-slug`, `--plan-path`, `--verbosity`
    - Load config for max_agents_concurrent
    - Call `runExecution(options)` with a stub dispatchAgent callback (actual dispatch happens from skill body)
    - Output result as JSON to stdout
    - Exit 0 on success, 1 on error

    **tests/orient/pipeline.test.ts**: Integration tests for the pipeline.
    - Mock all sub-modules (clarification, research, analysis, planner, validation)
    - Test: full pipeline with noConfirm produces scope contract + plan artifacts
    - Test: pipeline with noClarify skips clarification step
    - Test: pipeline calls onGate for scope and plan when noConfirm is false
    - Test: pipeline creates execution directory and plans directory
    - Test: slugifyTask produces valid slugs with timestamp suffix
    - Test: slugifyTask handles edge cases (empty string, very long task, special characters)
  </action>
  <verify>
    <automated>npx vitest run tests/orient/pipeline.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/orient/pipeline.ts exports: runOrientPipeline, slugifyTask
    - src/orient/pipeline.ts imports from clarification.ts, analysis.ts, research.ts, planner.ts, validation.ts
    - src/orient/run-orient.ts exists and contains `process.argv` parsing with `--phase` flag handling
    - src/orient/run-orient.ts supports `--phase` values: clarification, scope-contract, research, analysis-and-planning
    - src/orient/run-orient.ts supports `--task-slug` and `--answers` flags for multi-step invocation
    - src/orient/run-orient.ts supports `--check-only` for bootstrap verification
    - src/execution/run-execution.ts exists and contains `process.argv` parsing and `runExecution` call
    - tests/orient/pipeline.test.ts has `describe('runOrientPipeline'` with at least 4 test cases
    - tests/orient/pipeline.test.ts has `describe('slugifyTask'` with at least 3 test cases
    - `npx vitest run tests/orient/pipeline.test.ts -x` exits 0
  </acceptance_criteria>
  <done>Orient pipeline orchestrates full flow from clarification through plan approval with two gates. CLI entry points exist for both orient and execution phases with --phase flag supporting multi-step skill body invocation (clarification, scope-contract, research, analysis-and-planning). Pipeline tracks 60s budget. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Full orient skill body replacing stub</name>
  <files>skills/orient/SKILL.md</files>
  <read_first>
    - skills/orient/SKILL.md (current stub)
    - skills/bootstrap/SKILL.md (existing full skill body for pattern reference -- how bootstrap skill invokes node commands and handles output)
    - skills/onboard/SKILL.md (existing full skill body for pattern reference)
    - src/orient/pipeline.ts (runOrientPipeline, slugifyTask -- created in Task 1)
    - src/orient/run-orient.ts (CLI entry point -- created in Task 1)
    - src/execution/run-execution.ts (CLI entry point -- created in Task 1)
    - .planning/phases/04-orient-and-execution-engine/04-CONTEXT.md lines 16-18 (D-01 two-gate pipeline)
    - .planning/phases/04-orient-and-execution-engine/04-CONTEXT.md lines 19-28 (D-02 through D-09 clarification/gate)
    - .planning/phases/04-orient-and-execution-engine/04-CONTEXT.md lines 39-47 (D-14 through D-18 plan gate)
    - .planning/phases/04-orient-and-execution-engine/04-UI-SPEC.md lines 82-161 (all copy -- gates, progress, empty states, error states)
    - .planning/phases/04-orient-and-execution-engine/04-RESEARCH.md lines 684-691 (skill-as-orchestrator pattern)
  </read_first>
  <action>
    **skills/orient/SKILL.md**: Replace the stub with the full orient skill body.

    **Frontmatter:**
    ```yaml
    ---
    name: orient
    description: Take a task description and autonomously research, plan, and execute code changes using graph-informed clarification, hybrid execution, and filesystem coordination.
    allowed-tools:
      - Bash
      - Read
      - Write
      - Edit
      - Glob
      - Grep
      - Agent
    ---
    ```

    **Skill body** (natural language prompt Claude executes):

    The skill body follows the established pattern from bootstrap/onboard skills: step-by-step instructions that Claude interprets and executes conversationally.

    ```markdown
    # /codescope:orient

    You are the orient pipeline orchestrator. Given a task description, you will autonomously research, plan, and execute code changes.

    **Task:** $ARGUMENTS

    ## Prerequisites

    First verify CodeScope has analyzed this codebase:

    1. Run: `node --import tsx/esm src/orient/run-orient.ts --project-root "$(pwd)" --task "$ARGUMENTS" --check-only`
    2. If the output indicates bootstrap is missing, tell the user: "CodeScope has not analyzed this codebase yet. Run `/codescope:bootstrap` first to build the knowledge graph."

    ## Step 1: Clarification and Scope Contract

    Run the orient pipeline for clarification:

    1. Run: `node --import tsx/esm src/orient/run-orient.ts --project-root "$(pwd)" --task "$ARGUMENTS" --phase clarification`
    2. Parse the JSON output.
    3. If `needsClarification` is true:
       - Present the questions to the user, grouped by topic.
       - Collect answers.
       - Run: `node --import tsx/esm src/orient/run-orient.ts --project-root "$(pwd)" --task "$ARGUMENTS" --phase scope-contract --task-slug "{taskSlug}" --answers "{answers JSON}"`
    4. If `needsClarification` is false:
       - The scope contract was auto-generated.

    Read the scope contract from the output path and present it to the user:

    ## Scope Contract

    {Display In Scope / Out of Scope / Affected Files count / High-risk files / Community boundaries}

    Approve this scope? [approve / edit / reject]

    - If **approve**: Display "**APPROVED** -- proceeding to research and planning." and continue to Step 2.
    - If **edit**: Ask what to change, update the scope contract, re-present.
    - If **reject**: Display "**REJECTED** -- returning to clarification with your feedback." Ask for feedback and restart Step 1 with the rejection reason.

    If `--no-confirm` was passed in $ARGUMENTS: skip this gate, display "Scope auto-approved (--no-confirm)." and continue.

    ## Step 2: Research

    1. Run: `node --import tsx/esm src/orient/run-orient.ts --project-root "$(pwd)" --task "$ARGUMENTS" --phase research --task-slug "{taskSlug}"`
    2. The research module identifies external libraries in scope and prepares research topics.
    3. If research topics exist, spawn a research sub-agent:
       - Use the Agent tool to create a research agent with the prompt from the research module output.
       - The agent should have access to: Read, WebFetch, Bash (for Context7 lookups).
       - Wait for the agent to complete and write research.md to the execution directory.
    4. If no external research needed, display: "All affected libraries are internal -- skipping external research. Proceeding to planning."

    ## Step 3: Analysis and Planning

    1. Run: `node --import tsx/esm src/orient/run-orient.ts --project-root "$(pwd)" --task "$ARGUMENTS" --phase analysis-and-planning --task-slug "{taskSlug}"`
    2. Analysis runs directly (graph operations, fast).
    3. The planner module prepares a planning prompt. Spawn a planner sub-agent:
       - Use the Agent tool with the planner prompt.
       - The agent produces an execution plan with agent assignments.
       - Wait for completion.
    4. Parse and validate the plan output.
    5. If validation found mechanical errors, they were auto-fixed. If structural warnings exist, note them.

    Read the execution plan from the output path and present it:

    ## Execution Plan

    {Display agents, wave assignments, execution order, estimated changes, conventions, hybrid strategy rationale, validation status}

    Approve this plan? [approve / edit / reject]

    - If **approve**: Display "**APPROVED** -- beginning execution." and continue to Step 4.
    - If **edit**: Ask what to change. Allow task-level modifications: remove tasks, reorder priority, change file assignments, add constraints. Log removed tasks in the plan's "## Removed by User" section. Re-validate. Re-present.
    - If **reject**: Display "**REJECTED** -- returning to scope contract." Return to Step 1.

    If `--no-confirm` was passed: skip this gate, display "Plan auto-approved (--no-confirm)." and continue.

    ## Step 4: Execution

    Display: "## Executing..."

    1. Run: `node --import tsx/esm src/execution/run-execution.ts --project-root "$(pwd)" --task-slug "{taskSlug}" --plan-path "{planPath}"`
    2. BUT: the run-execution.ts prepares invocations. You must actually dispatch the agents:

    For each wave in the execution plan:
    - Display: "Executing wave {N}/{total}: [{agent names}]..."
    - For each agent in the wave:
      - Read the agent's invocation from the execution output.
      - Spawn the agent using the Agent tool with the prepared prompt, tool access, and timeout.
      - On completion: display "{agent-name} complete ({duration}s, {N} files)"
      - On failure: display "{agent-name} **failed** ({error}) -- retrying once..."
        - Retry with the same prompt + error context.
        - If retry fails: display "{agent-name} **failed** after retry -- skipping + {N} dependents"
    - After each wave completes, check for failed agents and skip their dependents in subsequent waves.

    ## Step 5: Summary

    After all waves complete, display the execution summary:

    ## Summary

    Total: {duration}s | Files changed: {N} | Agents: {succeeded}/{total} | Mode: {mode} | Tokens: ~{estimate}

    Next: Proceeding to verification... (Phase 5)

    If there were failures, also display the failure details and suggested next action.

    Partial results are in your working tree as uncommitted changes. Review with `git diff`.
    ```

    **Key patterns from existing skills:**
    - Use `$ARGUMENTS` for the task description
    - Run node commands via Bash tool with `node --import tsx/esm`
    - Parse JSON output from CLI entry points
    - Present gates conversationally (display content, ask for response)
    - Spawn sub-agents via Agent tool for research and planning
    - Execute agents step by step in the skill body (not in a subprocess)
  </action>
  <verify>
    <automated>grep -q "orient" skills/orient/SKILL.md && grep -q "Agent" skills/orient/SKILL.md && grep -q "run-orient" skills/orient/SKILL.md && grep -q "run-execution" skills/orient/SKILL.md && grep -q "\-\-phase" skills/orient/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - skills/orient/SKILL.md frontmatter has `name: orient` and `description:` containing "autonomous"
    - skills/orient/SKILL.md contains "Step 1" through "Step 5" section headings
    - skills/orient/SKILL.md contains "Approve this scope?" gate prompt
    - skills/orient/SKILL.md contains "Approve this plan?" gate prompt
    - skills/orient/SKILL.md contains "--no-confirm" handling
    - skills/orient/SKILL.md contains "--no-clarify" handling (either in body or via run-orient.ts args)
    - skills/orient/SKILL.md contains "run-orient.ts" reference for node command
    - skills/orient/SKILL.md contains "--phase" in run-orient.ts invocations
    - skills/orient/SKILL.md contains "run-execution.ts" reference for execution phase
    - skills/orient/SKILL.md contains "Agent tool" or "Agent" for sub-agent spawning
    - skills/orient/SKILL.md is longer than 50 lines (not a stub)
  </acceptance_criteria>
  <done>Full orient skill body replaces the stub. Skill orchestrates the complete pipeline conversationally: clarification with graph-informed questions, Gate 1 for scope approval, research sub-agent via Agent tool, planning sub-agent, validation, Gate 2 for plan approval, and wave-based execution dispatching agents via Agent tool. All run-orient.ts invocations use --phase flag to control pipeline step.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify full orient and execution pipeline</name>
  <files>skills/orient/SKILL.md, src/orient/pipeline.ts, src/execution/orchestrator.ts</files>
  <action>
    Human verification of the complete Phase 4 implementation across all 6 plans. This checkpoint confirms the full /codescope:orient pipeline works as designed with all modules wired correctly.
  </action>
  <verify>
    <automated>npx vitest run tests/orient/ tests/execution/ tests/onboard/ tests/config/ --reporter=verbose</automated>
  </verify>
  <done>All Phase 4 modules verified: orient pipeline (clarification, research, analysis, planning, validation), execution engine (coordination, teams detection, wave scheduling, agent spawning, orchestration), config updates (D-44), and onboarding agent teams detection (ONBD-06). Full test suite passes.</done>
</task>

</tasks>

<verification>
- `npx vitest run` full test suite passes (orient + execution + existing tests)
- Orient skill body is conversational (gates, progress messages)
- Pipeline creates execution/{taskSlug}/ directory and plans/{taskSlug}.md
- All module imports resolve correctly (no circular dependencies)
</verification>

<success_criteria>
- /codescope:orient [task] triggers the full pipeline via the orient skill body
- Pipeline handles two gates (scope, plan) with approve/edit/reject flows
- --no-confirm and --no-clarify flags work correctly
- run-orient.ts supports --phase flag with values: clarification, scope-contract, research, analysis-and-planning
- Research and planning dispatch as sub-agents via Agent tool
- Execution dispatches agents per wave schedule with teams detection
- All artifacts written to disk regardless of outcome (D-16)
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/04-orient-and-execution-engine/04-06-SUMMARY.md`
</output>
