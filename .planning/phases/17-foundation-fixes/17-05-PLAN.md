---
phase: 17-foundation-fixes
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/artifacts/generator.test.ts
autonomous: true
gap_closure: true
requirements:
  - GRAPH-01
  - GRAPH-02
  - GRAPH-03
  - GRAPH-04
  - GRAPH-05
  - GRAPH-06
  - CONV-01
  - CONV-02
  - PLUG-01
  - PLUG-02
  - PLUG-03

must_haves:
  truths:
    - "buildConventionIndex test 'parses conventions.md and maps conventions to their matchingFiles' passes"
    - "All 12 tests in tests/artifacts/generator.test.ts pass"
    - "Full regression suite shows no new failures compared to pre-gap-closure baseline (1232 pass)"
  artifacts:
    - path: "tests/artifacts/generator.test.ts"
      provides: "Updated createMockConventions() fixture using h3+table format"
      contains: "### Try-catch with typed errors"
  key_links:
    - from: "tests/artifacts/generator.test.ts"
      to: "src/conventions/parser.ts"
      via: "buildConventionIndex calls parseDetectorConventions internally"
      pattern: "parseDetectorConventions"
---

<objective>
Fix the test regression introduced by Plan 03: update the mock conventions fixture in tests/artifacts/generator.test.ts to use h3+table format that the canonical parser (parseDetectorConventions) expects.

Purpose: Plan 03 unified all convention parsing through src/conventions/parser.ts (parseDetectorConventions) but missed updating this test fixture, causing 1 test failure. The mock data uses the old bold-field format (**Convention:** name) which the new parser ignores.

Output: Updated test fixture, all 12 generator tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/conventions/parser.ts
@tests/artifacts/generator.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update createMockConventions() fixture to h3+table format</name>
  <files>tests/artifacts/generator.test.ts</files>
  <read_first>
    - tests/artifacts/generator.test.ts (the file being modified — see createMockConventions at line 95)
    - src/conventions/parser.ts (the canonical parser — understand exact format it expects)
  </read_first>
  <action>
Replace the `createMockConventions()` function body (lines 95-121) in tests/artifacts/generator.test.ts. The old format uses bold-field lines (`**Convention:** name`, `**Adoption:** 85%`, etc.) which parseDetectorConventions ignores. Replace with h3+table format that parseDetectorConventions parses.

New fixture content for `conventionsContent`:

```
# Detected Conventions

### Try-catch with typed errors

| Metric | Value |
|--------|-------|
| Adoption | 85% (17/20 files) |
| Confidence | HIGH-CONF |
| Trend | Stable |
| Category | error-handling |

**Evidence:**
- `src/auth.ts:10` -- Uses typed catch block
- `src/db.ts:15` -- Wraps query in try-catch

### Named imports over default

| Metric | Value |
|--------|-------|
| Adoption | 92% (23/25 files) |
| Confidence | HIGH-CONF |
| Trend | Stable |
| Category | imports |

**Evidence:**
- `src/index.ts:1` -- Uses named import
- `src/auth.ts:1` -- Uses named import
```

Key format rules the canonical parser requires (from src/conventions/parser.ts):
1. Convention names are `### ` h3 headings (parser splits on `^### ` at line 58)
2. Metric rows are markdown table format: `| Adoption | 85% (17/20 files) |` (parser matches `^\|\s*Adoption\s*\|` at line 75)
3. Confidence row: `| Confidence | HIGH-CONF |` (matched at line 78)
4. Category row: `| Category | error-handling |` (matched at line 82)
5. Evidence lines use backtick format: `- \`src/file.ts:15\` -- description` (parser matches `trimmed.startsWith("- \`")` at line 84)
6. Files are extracted from evidence lines via regex `\`([^:]+):` (line 87) — NOT from a separate "Files:" field

The test assertions at lines 220-240 expect:
- `result.files["src/auth.ts"]` has length 2 (appears in evidence of BOTH conventions)
- `result.files["src/db.ts"]` has length 1 with name "Try-catch with typed errors"
- `result.files["src/utils.ts"]` has length 1 with name "Named imports over default"

IMPORTANT: The old format had `src/utils.ts` in the "Files:" list for "Named imports over default" but NOT in Evidence lines. The canonical parser only extracts files from evidence lines (backtick format). So to make the test assertion at line 229 pass (`result.files["src/utils.ts"]` defined with 1 entry), you MUST add `src/utils.ts` as an evidence line in "Named imports over default":

```
- `src/utils.ts:3` -- Uses named import
```

Do NOT change any test assertions — only the fixture data. The assertions define the expected behavior; the fixture must produce data that satisfies them.
  </action>
  <verify>
    <automated>npx vitest run tests/artifacts/generator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "### Try-catch with typed errors" tests/artifacts/generator.test.ts returns 1
    - grep -c "### Named imports over default" tests/artifacts/generator.test.ts returns 1
    - grep -c "| Adoption |" tests/artifacts/generator.test.ts returns 2
    - grep -c "| Confidence | HIGH-CONF |" tests/artifacts/generator.test.ts returns 2
    - grep -c "| Category |" tests/artifacts/generator.test.ts returns 2
    - grep "src/auth.ts:10" tests/artifacts/generator.test.ts matches at least 1 line with backtick format
    - grep "src/db.ts:15" tests/artifacts/generator.test.ts matches at least 1 line with backtick format
    - grep "src/utils.ts:" tests/artifacts/generator.test.ts matches at least 1 evidence line with backtick format
    - npx vitest run tests/artifacts/generator.test.ts shows 12 passed, 0 failed
    - Old bold-field format strings (**Convention:**, **Adoption:**, **Confidence:**, **Category:**, **Files:**) do NOT appear in createMockConventions function
  </acceptance_criteria>
  <done>All 12 tests in tests/artifacts/generator.test.ts pass. The createMockConventions() fixture uses h3+table format matching what parseDetectorConventions expects. No test assertions were changed — only the fixture data was updated to match the new canonical format.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/artifacts/generator.test.ts` — all 12 tests pass (0 failures)
2. `npx vitest run` — full regression suite shows at least 1233 pass (1232 baseline + 0 new, with the 1 regression fixed)
3. No new test failures introduced
</verification>

<success_criteria>
- The "parses conventions.md and maps conventions to their matchingFiles" test passes
- All 12 generator.test.ts tests pass
- Full regression suite does not show any new failures beyond the 7 pre-existing ones (manifest 4 + dashboard 3)
- createMockConventions uses h3+table format, not old bold-field format
</success_criteria>

<output>
After completion, create `.planning/phases/17-foundation-fixes/17-05-SUMMARY.md`
</output>
